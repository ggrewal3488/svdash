/*!
 * qrcode-generator 1.4.4 (MIT License)
 * Copyright (c) Kazuhiko Arase
 * NOTE: Download from https://unpkg.com/qrcode-generator@1.4.4/qrcode.js
 * and save as qrcode.min.js — or run: npm run fetch-qr
 * 
 * Stub below renders a placeholder until the real library is bundled.
 */
var qrcode = function(typeNumber, errorCorrectLevel) {
  return {
    addData: function(data) { this._data = data; },
    make: function() { this._made = true; },
    getModuleCount: function() { return 25; },
    createImgTag: function(cellSize, margin) {
      // Fallback: render as text QR placeholder
      var s = cellSize || 4;
      return '<div style="width:' + (25*s) + 'px;height:' + (25*s) + 'px;' +
        'background:#fff;display:flex;align-items:center;justify-content:center;' +
        'font-size:10px;color:#333;text-align:center;padding:4px;border:1px solid #ddd">' +
        'QR: ' + (this._data || '').substring(0,20) + '…</div>';
    }
  };
};

// Self-remove and reload real library if network available
(function() {
  var s = document.createElement('script');
  s.src = 'https://unpkg.com/qrcode-generator@1.4.4/qrcode.js';
  s.onload = function() { console.log('[SVDash] QR library loaded from CDN'); };
  document.head.appendChild(s);
})();
