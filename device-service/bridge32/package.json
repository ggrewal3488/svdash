{
  "name": "godrej-bridge32",
  "version": "0.1.0",
  "private": true,
  "description": "32-bit hardware bridge for the Godrej RD-Z08 lock programmer (CH375DLL.DLL) and RW-41 card encoder (ACR120U.dll). Must run under a 32-bit (ia32) Node.js — the vendor DLLs are 32-bit and cannot load into a 64-bit process. See ../docs/VENDOR_SDK_FINDINGS.md.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "verify": "node verify.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "koffi": "^2.9.0"
  }
}
